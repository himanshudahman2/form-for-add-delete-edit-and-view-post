<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All posts</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="container">
        <h1 class="my-4 text-center">Quora Posts</h1>
        <div class="d-flex flex-wrap justify-content-center">
            <% for (post of posts) { %>
            <div class="card m-3" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">@<%=post.username %></h5>
                    <p class="card-text">@<%=post.content %></p>
                    <a href="http://localhost:8080/posts/<%=post.id%>" class="btn btn-primary">See in detail</a>
                    <a href="http://localhost:8080/posts/<%=post.id%>/edit" class="btn btn-warning">Edit</a>
                    <form action="/posts/<%=post.id%>?_method=DELETE" method="post" class="mt-2">
                        <button type="submit" class="btn btn-danger">Delete Post</button>
                    </form>
                </div>
            </div>
            <% } %>
        </div>
        <div class="text-center mt-4">
            <a href="http://localhost:8080/posts/new" class="btn btn-success">Create new post</a>
        </div>
    </div>
</body>
</html>
